{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Ejecutar App",
            "type": "debugpy",
            "request": "launch",
            "module": "app.main",
            "cwd": "${workspaceFolder}"
        }
    ]
}
